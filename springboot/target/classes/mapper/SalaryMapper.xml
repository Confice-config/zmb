<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mapper.SalaryMapper">

    <insert id="insert">
        insert into salary (employee_id, base_salary, bonus, performance, month)
        values (#{employeeId}, #{baseSalary}, #{bonus}, #{performance}, #{month})
    </insert>

    <update id="updateById">
        update salary
        set employee_id = #{employeeId},
            base_salary = #{baseSalary},
            bonus = #{bonus},
            performance = #{performance},
            month = #{month}
        where id = #{id}
    </update>

    <select id="selectAll" resultType="com.example.enetity.Salary">
        select * from salary
        <where>
            <if test="employeeId != null">
                employee_id = #{employeeId}
            </if>
            <if test="month != null and month != ''">
                AND month = #{month}
            </if>
        </where>
        order by id ASC
    </select>
    <select id="selectEeAll" resultType="com.example.enetity.Salary">
        select * from `salaryt`
    </select>
</mapper>